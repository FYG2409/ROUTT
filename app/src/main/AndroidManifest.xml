<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.routt.ara.routt">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Referencia"
            android:label="@string/title_activity_referencia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.Trailero.ContenedorTraileroActivity" />
        <activity android:name=".view.Ofertante.CreaViajeActivity" />
        <activity android:name=".CreaTraileroActivity" />
        <activity android:name=".SeleccionaTipoActivity" />
        <activity android:name=".CreaOfertanteActivity" />
        <activity android:name=".view.Ofertante.ContenedorOfertanteActivity" />
        <activity android:name=".view.InfoViaje"></activity>
    </application>

</manifest>